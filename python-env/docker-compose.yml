version: '0.0.1'
services:
  celery_test:
    restart: always
    build: .
    container_name: 'celery-test'
    working_dir: '/var/work'
    tty: true
    ports:
      - 8888:8888
    volumes:
      - ./work:/var/work
